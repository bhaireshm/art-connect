// https://nextjs.org/docs/app/building-your-application/authentication
// https://github.com/bhaireshm/art-connect/issues/18
// https://github.com/vercel/examples/tree/main/edge-middleware/jwt-authentication
// https://nextjs.org/learn/dashboard-app/adding-authentication

// Update the token in header's cookie
// const response = NextResponse.next();
// response.cookies.set(COOKIE.name, token, COOKIE.serializeOptions); //  cookie.serialize(COOKIE.name, token, COOKIE.serializeOptions));
// response.headers.append("Set-Cookie", cookie.serialize(COOKIE.name, token, COOKIE.serializeOptions));
// cookies().set(COOKIE.name, token, COOKIE.serializeOptions);
